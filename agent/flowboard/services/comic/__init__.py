"""Comic-panel pipeline services.

Built on top of the existing Flowboard primitives — node storage, the Flow
extension bridge, and the local media cache — without modifying any of them.
See ``bridge.py`` for the image-edit seam shared by the comic nodes.
"""
