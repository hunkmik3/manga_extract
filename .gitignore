# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
dist/
dist-ssr/
.vite/
*.local

# Editors
.vscode/
.idea/
*.swp
.DS_Store

# Env
.env
.env.local
.env.*.local

# Data
storage/
*.db
*.db-journal
*.sqlite
*.sqlite3

# Build
build/
coverage/
.turbo/
**/tsconfig.tsbuildinfo

# PyInstaller desktop build (packaging/flowboard.spec → dist/ + build/, ignored
# above) + the zipped bundle produced locally or in CI
flowboard-*.zip

# OMC ephemeral state (session caches, runtime checkpoints) — plans/ kept
.omc/state/

# Local curl evidence for Veo model dispatch — referenced by flow_sdk.py
# comments as source-of-truth, but contains raw OAuth bearer tokens.
# Keep on disk; never commit.
/video_model*.md
